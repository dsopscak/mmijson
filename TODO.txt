BUG: allows trailing comma at end of map or array, like [1,2,3,]. Fix
probably in pare_into_* functions.

BUG: doesn't support unicode escape sequences

BUG: segfaults on strings >= 1024 (the working buffer increment)
